export enum Roles {
  USER = 'user',
  ADMIN = 'admin',
  MASTER = 'master',
}

export enum AccountStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum UserClassStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
}
