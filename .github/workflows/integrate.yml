name: client/server Test CI

on:
  pull_request:
    branches: [master]

jobs:
  run_lerna_test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 16
      - run: npm ci
      - run: npm run test
